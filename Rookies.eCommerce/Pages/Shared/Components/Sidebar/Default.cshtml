@model List<Rookies.eCommerce.Domain.Category>
@{
}
<div class="">
    <div class="list-group">
        @foreach (var item in Model)
        {
            <a class="list-group-item" asp-page="/Product/Index" asp-route-id="@item.Id">@item.Name</a>

        }

    </div>
</div>
